---
context: default
phase: ddharmon 1.0.0 GUI buildout (ddharmon-ui)
task: 7
total_tasks: 7
status: paused
last_updated: 2026-07-01T21:35:42Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **do NOT overwrite the CORE repo's handoff** — `ph-arpa-data-harmonization/.planning/.continue-here.md`
  + `HANDOFF.json` track a **different active thread** (the v1.0.0/v3 pipeline + transform-spec work, "still in
  flux"). This GUI handoff lives in the **ddharmon-ui** repo on purpose. Keep them separate.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| GUI re-implements the pipeline | The v1 runner drove the pipeline stage-by-stage → every ddharmon change broke it. | advisory | Fixed: `backend/engine/adapter.py` calls `harmonize_leanb` and **injects** per-stage callbacks; maps `LeanBRecord`→`UIRecord` in ONE function. Keep all ddharmon imports confined to `engine/adapter.py`. |
| recharts SSR bundled as ESM | `react-dom/server` does a dynamic `require('util')` that ESM output can't handle. | advisory | Bundle the SSR/screenshot harness with esbuild `--format=cjs`, not esm. |
| Mistaking `cde-mapper-dashboard` for the workbench | That BIDS repo is a project *progress monitor*, not the mapping UI. | advisory | The candidate-review workbench idiom is CDEMapper **JAMIA 2025 Fig 4** (in `docs/references/Wang et al. …pdf`). |

<current_state>
The GUI buildout for **ddharmon 1.0.0** is COMPLETE through P0 (backend v2 rewire behind a stable contract),
P1 (frontend on the contract), P2 (biomapper-ui shared design system), and all **5 requested visualizations**
(match-journey Sankey, analytics overview, enriched review row, candidate-review workbench, embedding atlas).
Everything is committed across both repos and green. The ONLY remaining 1.0 item is the user-gated **P3 deploy**.

On branch `feature/v2-contract-and-viz` (off `main`) in ddharmon-ui. Nothing pushed yet.
</current_state>

<completed_work>

- P0 backend: `engine/contract.py` (UIRecord/UIResult boundary) + `engine/adapter.py` (drives harmonize_leanb via injected sync/batch/preview callbacks); runner/app/jobs on the contract; runMode batch-default; contract-based export. 9 tests incl. real e2e (LLM stages mocked). — commit `4fff6e4`
- P1 frontend: `types.ts`/`api.ts` mirror; Configure / Run (data-driven progress) / Review views. — `e1bf4d8`
- P2 styling: sidebar+topbar chrome, bordered cards, breadcrumb — screenshot-verified. — `e1bf4d8`
- Viz (all Playwright-verified): Sankey, analytics (coverage table + bars + cosine histogram + overlap heatmap), enriched review row, candidate-review workbench (`/job/:id/workbench`), embedding atlas. — `e1bf4d8`
- CORE additive change: `CandidateCDE` + `candidates` on `LeanBRecord`, persisted in `assemble_leanb` (+regression test; 23 leanb tests green). — core commit `f5802a9` on `feature/harmonization-v2-split-aware`
</completed_work>

<remaining_work>

- **Task 7 — P3 deploy (user-gated):** make ddharmon-ui deployed (Lightsail + Squarespace subdomain; systemd + nginx + certbot per `deploy/README.md`), repin the `ddharmon` dep from private git to public PyPI, flip the repo public. This is the **coupled 1.0.0 gate** — the `ddharmon 1.0.0` PyPI tag waits on it.
- Optional / deferred: push the two branches + open PRs; workbench extras (⌘K palette, batch mode, resizable panels); ItemComplex sunburst + n_cohorts TreeMap overview.
</remaining_work>

<decisions_made>

- GUI couples to ddharmon only through `engine/` (adapter injects callbacks into `harmonize_leanb`) — so pipeline churn doesn't force GUI changes.
- Run mode = Batch default + sync + preview(no-LLM). v2 requires a CDE catalog (no `cdeSet=none`).
- Shared PH design system from biomapper-ui (tokens already identical → P2 = component/layout parity).
- Candidates surfaced via an ADDITIVE `CandidateCDE` field (backward-compatible). Atlas computed in the adapter (PCA), no core change.
</decisions_made>

<blockers>
- P3 deploy: needs Lightsail + Squarespace access (infra) — human-gated.
- Repin: needs ddharmon 0.7.0+ on PyPI first (currently held; repo pins private git).
</blockers>

## Required Reading (in order)
1. Memory `project_ddharmon_ui_v2_buildout.md` — the full, current state of this thread.
2. `ddharmon-ui/docs/GUI-BUILD-PLAN.md` — the insulation architecture (the governing design).
3. `ph-arpa-data-harmonization/.planning/V1.0-ROADMAP.md` — 1.0 scope; P3 deploy is the coupled gate.
4. `ddharmon-ui/deploy/README.md` — the Lightsail + Squarespace runbook (for P3).

## Infrastructure State
- No running servers (vite dev killed).
- **ddharmon-ui frontend:** `node_modules` installed; run `npm run typecheck` / `npm run build` from `frontend/`.
- **ddharmon-ui backend:** NO local venv — run tests with the CORE repo's venv: from `ddharmon-ui/`, `PYTHONPATH=. /Users/bhargav/ai-coding/ph-arpa-data-harmonization/.venv/bin/python -m pytest tests/`; ruff/black/pyright via that same venv's bin.
- Playwright chromium installed (used for the screenshot verification harness).
- biomapper-ui cloned at `~/ai-coding/biomapper-ui` (branch `dev`) — the design-system reference.

<context>
The whole session honored one governing constraint: "changes to the ddharmon flow shouldn't force GUI changes."
That's implemented as a single seam (`engine/contract.py` boundary + `engine/adapter.py` the only ddharmon
importer). All the viz is derived from the `UIResult` contract, so nothing new couples to the pipeline. Work is
committed + green; the natural resume is either pushing/PRing the branches or starting P3 deploy.
</context>

<next_action>
Start with: confirm intent — (a) push `feature/v2-contract-and-viz` (ddharmon-ui) + `feature/harmonization-v2-split-aware` (core) and open PRs, or (b) begin P3 deploy: read `ddharmon-ui/deploy/README.md`, then Lightsail provision + PyPI repin. All code is committed and green; no code work is pending.
</next_action>
