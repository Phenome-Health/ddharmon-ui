---
context: default
phase: ddharmon-ui v1.5 — BYOK backend (thread x-anthropic-key into the pipeline)
task: built + tested; on feature/byok-backend (PR #3)
total_tasks: built + tested
status: BYOK backend BUILT + GREEN (17 tests) on feature/byok-backend — PR #3; live only after P3 deploy
last_updated: 2026-07-05T00:00:00Z
---

# ddharmon-ui — v1.5: BYOK backend BUILT (feature/byok-backend, PR #3)

**Prod:** https://ddharmon.netlify.app/ (static/backend-less — BYOK header sent-but-ignored until P3).
**Branch:** `feature/byok-backend` @ `f315d12` in `~/ai-coding/ddharmon-ui` (off `main` @ `fe40703`). The v1.1–v1.4
arc is merged (PR #2). This session added the **BYOK backend**: the frontend already sent the `x-anthropic-key`
header (v1.4); the backend now threads it end-to-end into the core Anthropic clients. Working tree clean.

## Critical Anti-Patterns (each discovered by an actual failure/finding this session)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| API key in the config body | The BYOK key must NOT go in the `config` JSON — that object is persisted as the job's `run_config` (`store.create`), so it would leak to disk/logs. | blocking (for the backend) | Key rides as a transport-only `x-anthropic-key` header only; held in memory for the job, never persisted. |
| Mutating `os.environ` for per-request keys | Core `anthropic.Anthropic()` reads `ANTHROPIC_API_KEY` from env in **4 places** (`src/ddharmon/llm/batch.py` ×3, `llm/anthropic_client.py:64`). Setting env per-request races + leaks keys across concurrent jobs. | blocking (for the backend) | Thread the key down to each client constructor as `api_key=...`; never touch process env. |
| Tailwind arbitrary `clamp()` minus operator | `clamp(1rem,16vh-117px,3.5rem)` is invalid CSS — `calc`-style math needs spaces around `-`. | advisory (fixed) | Use underscores in the arbitrary value: `lg:py-[clamp(1rem,16vh_-_117px,3.5rem)]` → Tailwind expands `_`→space. |
| Force-fit-to-viewport layout | Pinning a multi-section page to 100vh / `flex-1` fill is fragile across zoom + browser chrome and needs magic pixel offsets. | advisory | Use fluid `clamp()` spacing keyed to vh; floors = the compact case, capped maxima. Let it scroll on genuinely tiny windows. |
| Playwright module resolution | `import "playwright"` from a scratchpad script fails — ESM resolves from the script's dir, and Playwright is only in `frontend/node_modules`. | advisory | Put screenshot scripts inside `frontend/` (or run from there) so the bare import resolves. |

<current_state>
On `feature/byok-backend` @ `f315d12`, working tree clean (ahead of `main`: code commit, then planning docs).
Nothing blocked. No servers running. The BYOK backend is **built + green (17 tests)**; PR #3 awaits review/merge.
It does not change prod behavior on its own — prod is static/backend-less, so the header is sent-but-ignored
until the **P3 Lightsail backend** deploys (the coupled 1.0 gate).
</current_state>

<byok_backend_this_session>
Threaded the per-request `x-anthropic-key` key end-to-end (the frontend already sent it in v1.4):
- `backend/app.py::start_batch` reads the `X-Anthropic-Key` header (FastAPI `Header()` param) and passes it as
  an **in-memory thread kwarg** — deliberately NOT into `run_config` (which `store.create` persists to disk).
- `backend/runner.py::run_harmonization` and `backend/engine/adapter.py::run_pipeline` gained `api_key=` and
  thread it into sync `AnthropicClient(api_key=)` and batch `_batch_stage` → `submit_and_wait(api_key=)`.
- **Core half** (research repo `feature/harmonization-v2-split-aware` @ `092bc94`): non-breaking optional
  `api_key: str | None = None` on `AnthropicClient` + `batch.submit_batch/retrieve_batch/submit_and_wait` →
  `anthropic.Anthropic(api_key=)` at all 4 sites; `None` keeps the `ANTHROPIC_API_KEY` env fallback (unchanged).
- **Security (test-locked):** transport-only header → thread kwarg → constructor arg; the key is NEVER in
  `run_config`/logs and never `os.environ` (no cross-job leakage). New test asserts the secret appears nowhere
  in the persisted job, and that an absent header → `api_key=None`. 17 backend tests pass.
- **Two gates to go live in PROD:** (1) the P3 backend deploy; (2) the core `api_key=` param reaching PyPI — it
  is a post-0.9.0 delta not yet in canonical/PyPI (forward-port with 0.9.1/1.0.0). Local dev already works (the
  UI `.venv` symlinks the core repo's editable `src/`, which has the param now).
</byok_backend_this_session>

<completed_work>
Prior session (v1.4, 5 tasks, committed + merged via PR #2):
- `74bbb46` **BYOK key field (frontend only)** — masked input + reveal toggle + `sk-ant-` validation on the
  new-run form, shown only for LLM run modes; `startHarmonize(files,config,apiKey?)` sends `x-anthropic-key`
  header (never in config body); in-memory only. + dropped the misleading "needs API key" from the Sync label.
- `661ce88` **landing fits above the fold on a 14" MBP** (was +309px overflow) — smaller hero padding/viz,
  wider text col, tighter spacing; global AppShell padding py-8→py-6. text-6xl headline kept.
- `cbc93d4` **landing fluid clamp() spacing** — hero padding/gap/card-padding keyed to vh, viz `min(500px,47vh)`;
  fills taller monitors (blank ~250px→~58px @2000×1047), capped so it never over-stretches.
- `fe40703` **pushed branch → PR #2 → MERGED to main**; local main synced. Pre-merge: typecheck+build green,
  16 backend tests pass, prod Playwright-verified at 1512×832 and 2000×1047.
</completed_work>

<remaining_work>
- **Merge PR #3** (BYOK backend) once reviewed — it's built + green, does not change prod on its own.
- **P3 Lightsail backend deploy** (user-gated, the coupled 1.0 gate) — where BYOK actually goes live. Also needs
  the core `api_key=` param on PyPI (post-0.9.0 forward-port) if the deployed backend pins the published package
  rather than the editable src.
- OPTIONAL now: delete stale merged branches `feature/ui-guide-tooltips-notebook-demo` + `feature/landing-hero`;
  triage 3 Dependabot alerts on main (1 high/1 mod/1 low, pre-existing).
- Deferred: v3 demo precompute; optional polish (topbar icon, Works Cited tweaks).
</remaining_work>

<decisions_made>
- **BYOK** (user enters own key) over shared-operator-key/hybrid — each user pays for their runs.
- Build the **frontend now**, backend later (static deploy + non-trivial core change).
- Key = transport-only `x-anthropic-key` header, in-memory only; NEVER in the persisted config.
- Landing = **fluid clamp()**, not 100vh force-fit (fragile under zoom/chrome).
- Merge commit for PR #2 (matches PR #1); **no core `Phenome-Health/ddharmon` PR** (UI-only).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. Memory `project_ddharmon_ui_v2_buildout.md` — full UI-thread state incl. the v1.1–v1.4 arc + BYOK + merge.
2. `.planning/HANDOFF.json` (this dir) — machine-readable state.
3. For the BYOK backend: `backend/app.py::start_batch`, `backend/engine/adapter.py`, and the 4 core client
   sites (`../ph-arpa-data-harmonization/src/ddharmon/llm/batch.py`, `llm/anthropic_client.py:64`).

## Infrastructure State
- **Prod LIVE:** https://ddharmon.netlify.app — Netlify CLI linked. Redeploy the working tree with
  `./scripts/deploy-preview.sh` (tsc gate → VITE_STATIC build → `netlify deploy --prod`; `--draft` = throwaway URL).
  The user can self-deploy by typing the command with a `!` prefix in chat.
- **No servers running.** Local dev: `DDHARMON_UI_SEED=1 DDHARMON_CDE_DIR=/Users/bhargav/ai-coding/ph-arpa-data-harmonization/data/examples ./dev.sh` → http://localhost:5173.
- Backend tests: `PYTHONPATH=. DDHARMON_CDE_DIR=<core>/data/examples .venv/bin/python -m pytest tests/` (16 pass).
  Local dev venv = symlink `ddharmon-ui/.venv` → core repo `.venv`. No CI workflows in the repo.
- **Two separate threads:** the UI thread lives in THIS repo's `.planning`; the pipeline thread (M2/M4/M5/M7/M10
  → held-out full-5, user-gated ~$25) is SEPARATE in `~/ai-coding/ph-arpa-data-harmonization/.planning`.

<context>
Resume → clarify BYOK vs shared key → build the frontend key field → clarify "where's the key asked for"
(nowhere; env-only) and "we need to ask users for real runs" → build the frontend half → fit the landing above
the fold on the 14" → answer "is dynamic scaling good practice?" (fluid yes, force-fit no) → fluid clamp →
push + PR #2 + merge to main. Everything verified against live prod. The user reviews as we go.
</context>

<next_action>
BYOK backend is done + green on `feature/byok-backend` (PR #3) — merge when reviewed. It goes live in PROD only
after **two** gates: (1) the P3 Lightsail backend deploy; (2) the core `api_key=` param reaching PyPI (post-0.9.0
forward-port). The natural next substantive thread is **P3** (drafting the deploy plan is agent-doable; standing
it up is user-gated infra). Cheap tidy-ups: delete the 2 stale merged branches; triage the 3 Dependabot alerts.
</next_action>
